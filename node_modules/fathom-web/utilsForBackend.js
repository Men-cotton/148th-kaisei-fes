'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.dirsIn = dirsIn;
exports.staticDom = staticDom;

var _fs = require('fs');

var _path = require('path');

var _oldApi = require('jsdom/lib/old-api');

/**
 * @return {String[]} The name (not path) of each directory directly within a
 *      given path
 */
function dirsIn(path) {
  return (0, _fs.readdirSync)(path).filter(f => (0, _fs.statSync)((0, _path.join)(path, f)).isDirectory());
}

/**
 * Parse an HTML doc, and return a DOM-compliant interface to it. Do not
 * execute any of its inline scripts.
 */
/**
 * Things that work only on a command-line node.js environment
 */

function staticDom(html) {
  return (0, _oldApi.jsdom)(html, { features: { ProcessExternalResources: false,
      FetchExternalResources: false } });
}